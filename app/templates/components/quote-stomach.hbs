<li class="quote">
  <q id={{id}}>{{quote}}</q>
  <small>-{{source}}</small>
  {{#unless isReal}}
    <div class="edit">
      <a href="#">EDIT</a>
      <a href="#" id="delete">DELETE</a>
    </div>
  {{/unless}}
</li>
