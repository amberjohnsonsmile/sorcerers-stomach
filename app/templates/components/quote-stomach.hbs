<li class="quote">
  <q>{{quote}}</q>
  <small>-{{source}}</small>
  {{#unless isReal}}
    <div class="edit">
      {{#link-to "edit" (query-params id=id)}}EDIT{{/link-to}}
      <a href="#" id="delete">DELETE</a>
    </div>
  {{/unless}}
</li>
